version: '3.8'

services:
  todo-app:
    build: .
    ports:
      - "8082:8082"
    environment:
      - TODO_PORT=8082
      - TODO_PASSWORD=secret123
    volumes:
      - todo-data:/data
    restart: unless-stopped

volumes:
  todo-data: